import { jsxs as _jsxs, jsx as _jsx } from "react/jsx-runtime";
import Button from '@material-ui/core/Button';
import { styled } from '@material-ui/styles';
import { readableColor } from 'polished';
import { useState } from 'react';
function CounterButton() {
    const [count, setCount] = useState(0);
    let increment = () => setCount(count + 1);
    return (_jsxs("div", { children: [_jsxs("p", { children: ["Counter: ", count] }, void 0),
            _jsx("button", Object.assign({ onClick: increment }, { children: "Increment" }), void 0)] }, void 0));
}
;
let MyButton = styled(Button)((props) => ({
    backgroundColor: (props.color && props.color) || 'green',
    color: readableColor(props.color),
    "&:hover": {
        color: readableColor(readableColor(props.color)),
    }
}));
let NoPropsButton = styled(Button)({
    backgroundColor: 'green',
});
export { MyButton, NoPropsButton, CounterButton };
